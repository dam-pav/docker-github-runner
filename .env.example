# The repository where the runner will be registered
REPO_URL=https://github.com/owner/repo

# GitHub PAT (generate from GitHub > Settings > Developer settings > Personal access tokens)
# The PAT must have `repo` scope for repository runners or `admin:org` / `repo` scope for org-level runners.
# You can set `GITHUB_TOKEN` here, or you may instead place the token in a
# host credentials file at `/etc/github-runner/credentials` (the file should
# contain a line like `GITHUB_TOKEN=ghp_xxx...`). When the file is present, its
# value takes priority over this environment variable and the variable may be
# left empty.
GITHUB_TOKEN=

# Optional: host directory containing the `credentials` file. Defaults to
# `/etc/github-runner`. When set, Compose will mount this path into the
# container at `/run/secrets`.
HOST_CRED_LOCATION=/etc/github-runner

# Required — must set a unique name for this runner (no default)
RUNNER_NAME=

# Optional: labels for the runner (comma-separated, no spaces) to be added to the built-in labels
# (self-hosted, linux, x64). Example: `label1,label
RUNNER_LABELS=

# GitHub API retry parameters (optional)
# Number of attempts for API calls (default 6)
GH_API_RETRIES=6
# Initial delay in seconds between retries (default 1)
GH_API_INITIAL_DELAY=1
# Exponential backoff multiplier (default 2)
GH_API_BACKOFF_MULT=2

